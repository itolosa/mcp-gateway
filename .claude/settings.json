{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -l -c 'cd \"$CLAUDE_PROJECT_DIR\" && cargo fmt --all 2>&1 && cargo clippy --all-targets -- -D warnings 2>&1'",
            "timeout": 120
          }
        ]
      }
    ]
  }
}

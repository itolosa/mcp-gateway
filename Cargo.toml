[package]
name = "mcp-gateway"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
indexing_slicing = "deny"

[profile.mutants]
inherits = "test"
debug = "none"

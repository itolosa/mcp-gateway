name: Release

on:
  workflow_call:

permissions:
  contents: write

env:
  CARGO_TARGET_DIR: target

jobs:
  tag:
    name: Create CalVer Tag
    runs-on: ubuntu-latest
    outputs:
      tag_name: ${{ steps.calver.outputs.tag_name }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Compute CalVer tag
        id: calver
        run: |
          BASE=$(date -u +%Y.%m.%d)
          TAG="$BASE"
          SUFFIX=0
          while git rev-parse "refs/tags/$TAG" >/dev/null 2>&1; do
            SUFFIX=$((SUFFIX + 1))
            TAG="${BASE}.${SUFFIX}"
          done
          echo "tag_name=$TAG" >> "$GITHUB_OUTPUT"

      - name: Create and push tag
        run: |
          git tag "${{ steps.calver.outputs.tag_name }}"
          git push origin "${{ steps.calver.outputs.tag_name }}"

  build:
    name: Build ${{ matrix.target }}
    needs: tag
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
          - target: x86_64-apple-darwin
            os: macos-latest
          - target: aarch64-apple-darwin
            os: macos-latest

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install mold linker
        if: runner.os == 'Linux'
        run: sudo apt-get install -y mold

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@779680da715d629ac1d338a641029a2f4372abb5 # v2
        with:
          key: ${{ matrix.target }}

      - name: Install cross-compilation tools
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Install musl tools
        if: matrix.target == 'x86_64-unknown-linux-musl'
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
          MCP_GATEWAY_VERSION: ${{ needs.tag.outputs.tag_name }}

      - name: Install cargo-about
        run: cargo install cargo-about

      - name: Generate third-party licenses
        run: cargo about generate about.hbs > THIRDPARTY

      - name: Package binary
        shell: bash
        run: |
          BIN=mcp-gateway
          ARCHIVE="${BIN}-${{ matrix.target }}.tar.gz"
          RELEASE_DIR="target/${{ matrix.target }}/release"
          cp LICENSE THIRDPARTY "${RELEASE_DIR}/"
          tar czf "${ARCHIVE}" -C "${RELEASE_DIR}" "${BIN}" LICENSE THIRDPARTY
          echo "ARCHIVE=${ARCHIVE}" >> $GITHUB_ENV

      - name: Upload artifact
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: ${{ matrix.target }}
          path: ${{ env.ARCHIVE }}

  release:
    name: Create Release
    needs: [tag, build]
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2
        with:
          tag_name: ${{ needs.tag.outputs.tag_name }}
          files: artifacts/**/*.tar.gz
          generate_release_notes: true

  update-tap:
    name: Update Homebrew Tap
    needs: [tag, release]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          sparse-checkout: .github/formula

      - name: Download release assets and compute checksums
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.tag.outputs.tag_name }}
        run: |
          REPO="itolosa/mcp-gateway"
          for target in aarch64-apple-darwin x86_64-apple-darwin x86_64-unknown-linux-gnu; do
            FILE="mcp-gateway-${target}.tar.gz"
            gh release download "$TAG" --repo "$REPO" --pattern "$FILE"
            SHA=$(sha256sum "$FILE" | awk '{print $1}')
            VAR="SHA_$(echo "$target" | tr '[:lower:]-' '[:upper:]_')"
            echo "${VAR}=${SHA}" >> "$GITHUB_ENV"
          done

      - name: Generate formula from template
        env:
          TAG: ${{ needs.tag.outputs.tag_name }}
        run: |
          sed -e "s|%%TAG%%|${TAG}|g" \
              -e "s|%%SHA_AARCH64_APPLE_DARWIN%%|${SHA_AARCH64_APPLE_DARWIN}|g" \
              -e "s|%%SHA_X86_64_APPLE_DARWIN%%|${SHA_X86_64_APPLE_DARWIN}|g" \
              -e "s|%%SHA_X86_64_UNKNOWN_LINUX_GNU%%|${SHA_X86_64_UNKNOWN_LINUX_GNU}|g" \
              .github/formula/mcp-gateway.rb.template > mcp-gateway.rb

      - name: Push formula to tap
        env:
          TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          TAG: ${{ needs.tag.outputs.tag_name }}
        run: |
          git clone "https://x-access-token:${TAP_TOKEN}@github.com/itolosa/homebrew-tap.git" tap
          mkdir -p tap/Formula
          cp mcp-gateway.rb tap/Formula/mcp-gateway.rb
          cd tap
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/mcp-gateway.rb
          git diff --cached --quiet && exit 0
          git commit -m "mcp-gateway ${TAG}"
          git push

name: CI

on:
  pull_request:
    branches: [main]
  workflow_call:
    inputs:
      update_badge:
        type: boolean
        default: false

permissions:
  contents: write
  security-events: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_TARGET_DIR: target

jobs:
  check:
    name: Check, Test, Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Install mold linker
        run: sudo apt-get install -y mold

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
        with:
          components: clippy, rustfmt, llvm-tools-preview

      - uses: Swatinem/rust-cache@779680da715d629ac1d338a641029a2f4372abb5 # v2

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@c1305996457413b456b636a549d9f98354fd89cf # cargo-llvm-cov

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Install cargo-audit
        uses: taiki-e/install-action@385db9cc6bf65d19775b02084a4b698eaca9a4f2 # v2
        with:
          tool: cargo-audit

      - name: Audit dependencies
        run: cargo audit

      - name: Install cargo-deny
        uses: taiki-e/install-action@385db9cc6bf65d19775b02084a4b698eaca9a4f2 # v2
        with:
          tool: cargo-deny

      - name: Check dependency policy
        run: cargo deny check

      - name: Build
        run: cargo build

      - name: Test with coverage
        run: cargo llvm-cov --ignore-filename-regex 'main\.rs' --fail-under-lines 100 --fail-under-functions 100

      - name: Generate LCOV report
        run: cargo llvm-cov report --lcov --ignore-filename-regex 'main\.rs' --output-path lcov.info

      - name: Upload coverage to Codacy
        uses: codacy/codacy-coverage-reporter-action@89d6c85cfafaec52c72b6c5e8b2878d33104c699 # v1
        with:
          project-token: ${{ secrets.CODACY_PROJECT_TOKEN }}
          coverage-reports: lcov.info

      - name: Extract coverage percentage
        if: inputs.update_badge
        id: coverage
        run: |
          LINES=$(cargo llvm-cov report --json --ignore-filename-regex 'main\.rs' | jq '.data[0].totals.lines.percent')
          echo "percentage=${LINES}" >> "$GITHUB_OUTPUT"
          if awk "BEGIN {exit !($LINES >= 100)}"; then echo "color=brightgreen" >> "$GITHUB_OUTPUT"
          elif awk "BEGIN {exit !($LINES >= 90)}"; then echo "color=green" >> "$GITHUB_OUTPUT"
          elif awk "BEGIN {exit !($LINES >= 80)}"; then echo "color=yellowgreen" >> "$GITHUB_OUTPUT"
          else echo "color=red" >> "$GITHUB_OUTPUT"; fi

      - name: Update coverage badge
        if: inputs.update_badge
        uses: action-badges/core@5a5deee35aff00eff6253764a5b252b5cba7923c # 0.4.0
        with:
          label: coverage
          message: "${{ steps.coverage.outputs.percentage }}%"
          message-color: "${{ steps.coverage.outputs.color }}"
          file-name: coverage.svg
          badge-branch: badges
          github-token: "${{ secrets.GITHUB_TOKEN }}"

  codeql:
    name: CodeQL Analysis
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@ae9ef3a1d2e3413523c3741725c30064970cc0d4 # v3
        with:
          languages: rust

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@ae9ef3a1d2e3413523c3741725c30064970cc0d4 # v3
